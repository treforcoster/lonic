import type { Plugin } from '@wordpress/plugins';
import type * as Filter from './filter.js';
export interface Props extends Plugin {
    name: string;
    filters?: Filter.KeyedFilters;
    settings: Record<string, any>;
}
export declare const isRegistered: (name: string) => boolean;
export declare const register: ({ name, filters, ...settings }: Props) => void;
export declare const unregister: ({ name, filters }: {
    name: any;
    filters: any;
}) => void;
//# sourceMappingURL=plugin.d.ts.map