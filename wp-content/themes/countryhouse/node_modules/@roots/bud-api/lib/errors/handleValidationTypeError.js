import { InputError } from '@roots/bud-support/errors';
import figures from '@roots/bud-support/figures';
/**
 * Handle malformed input
 */
export function handleTypeError(_bud, label, { error }) {
    throw new InputError(`${label}: `.concat(error.issues
        .map(issue => `${issue.path} ${figures.arrowRight} ${issue.message}`)
        .join(`\n`)), {
        props: {
            thrownBy: label,
            docs: new URL(`https://bud.js.org/docs/${label}`),
        },
    });
}
//# sourceMappingURL=handleValidationTypeError.js.map