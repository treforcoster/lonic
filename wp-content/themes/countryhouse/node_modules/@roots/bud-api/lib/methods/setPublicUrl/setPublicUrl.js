export const setPublicUrl = function (url) {
    const normalUrl = !(url instanceof URL) ? new URL(url) : url;
    this.hooks.on(`dev.publicUrl`, normalUrl);
    return this;
};
//# sourceMappingURL=setPublicUrl.js.map