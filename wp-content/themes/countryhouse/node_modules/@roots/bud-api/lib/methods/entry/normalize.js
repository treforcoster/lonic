export const normalizeRecord = (data) => Object.entries(data).reduce((entries, [signifier, item]) => ({
    ...(entries ?? {}),
    [signifier]: {
        import: normalizeImport(item),
    },
}), {});
export const normalizeImport = (data) => {
    return Array.isArray(data) ? data : [data];
};
//# sourceMappingURL=normalize.js.map