export const setPublicProxyUrl = function (url) {
    const normalUrl = !(url instanceof URL) ? new URL(url) : url;
    this.hooks.on(`dev.publicProxyUrl`, normalUrl);
    return this;
};
//# sourceMappingURL=setPublicProxyUrl.method.js.map