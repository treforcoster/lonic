export const watch = async function (input) {
    if (!this.isDevelopment)
        return this;
    this.hooks.on(`dev.watch.files`, files => {
        const watchlist = files === undefined ? new Set() : files;
        (Array.isArray(input) ? input : [input]).map(file => {
            watchlist.add(file);
        });
        return watchlist;
    });
    return this;
};
//# sourceMappingURL=index.js.map