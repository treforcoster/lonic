import { __decorate } from "tslib";
import { Extension } from '@roots/bud-framework/extension';
import { label } from '@roots/bud-framework/extension/decorators';
/**
 * resolve-url-loader configuration
 */
let BudResolveUrl = class BudResolveUrl extends Extension {
    /**
     * {@link Extension.register}
     */
    async register(bud) {
        bud.build
            .setLoader(`resolveUrl`, await this.resolve(`resolve-url-loader`, import.meta.url))
            .setItem(`resolveUrl`, {
            loader: `resolveUrl`,
            options: ({ path }) => ({ root: path(`@src`), sourceMap: true }),
        });
    }
};
BudResolveUrl = __decorate([
    label(`@roots/bud-sass/resolve-url`)
], BudResolveUrl);
export { BudResolveUrl };
//# sourceMappingURL=extension.js.map