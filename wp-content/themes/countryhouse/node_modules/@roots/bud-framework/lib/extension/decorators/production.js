/**
 * Extension to be run in `production` mode
 */
export const production = (constructor) => class extends constructor {
    constructor(...args) {
        super(...args);
        this.enabled = this.app.isProduction;
    }
};
//# sourceMappingURL=production.js.map