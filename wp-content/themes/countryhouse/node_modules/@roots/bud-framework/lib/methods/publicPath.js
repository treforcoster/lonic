import { join } from 'node:path';
/**
 * Public path
 *
 * @remarks
 * Path from web root to assets
 */
export const publicPath = function (...parts) {
    const ctx = this;
    const basePath = ctx.hooks.filter(`build.output.publicPath`, `auto`);
    if (!parts.length)
        return basePath;
    if (basePath !== `auto`)
        return join(basePath, ...parts);
    return join(...parts);
};
//# sourceMappingURL=publicPath.js.map