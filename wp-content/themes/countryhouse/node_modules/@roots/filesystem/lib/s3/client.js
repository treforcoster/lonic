/**
 * S3 client
 */
export class Client {
    /**
     * Make a new client
     *
     * @param config - {@link S3ClientConfig}
     * @returns {@link S3Client}
     */
    async make(config) {
        return await import(`@aws-sdk/client-s3`).then(({ S3 }) => new S3(config));
    }
}
//# sourceMappingURL=client.js.map