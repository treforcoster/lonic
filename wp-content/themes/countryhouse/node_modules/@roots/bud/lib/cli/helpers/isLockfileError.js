import isUndefined from '@roots/bud-support/lodash/isUndefined';
export const isLockConflict = (bud) => !isUndefined(bud.context.files?.[`yarn.lock`]) &&
    !isUndefined(bud.context.files?.[`package-lock.json`]);
export const isNoLock = (bud) => !bud.context.files?.[`yarn.lock`] &&
    !bud.context.files?.[`package-lock.json`] &&
    !bud.context.files?.[`package.json`]?.module?.packageManager;
export const lockError = (bud) => isLockConflict(bud) || isNoLock(bud);
//# sourceMappingURL=isLockfileError.js.map