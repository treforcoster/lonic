import { isLockConflict } from './isLockfileError.js';
export const checkPackageManagerErrors = (bud) => {
    if (!bud.context.files)
        return false;
    if (isLockConflict(bud)) {
        return true;
    }
    return false;
};
//# sourceMappingURL=checkPackageManagerErrors.js.map